{
  "name": "automation",
  "version": "1.0.0",
  "private": true,
  "description": "Worker de automatización de navegador para captura de candidatos (Indeed / Computrabajo). Corre local, nunca en Vercel/Railway.",
  "scripts": {
    "probe:indeed": "tsx src/spike/indeed-probe.ts",
    "probe:computrabajo": "tsx src/spike/computrabajo-probe.ts"
  },
  "dependencies": {
    "playwright": "^1.49.0"
  },
  "devDependencies": {
    "@types/node": "^20.14.0",
    "tsx": "^4.19.0",
    "typescript": "^5.4.5"
  }
}
